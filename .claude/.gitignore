# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
!.vscode/extensions.json
*.swp
*.swo

# Secrets — кожен starter мусить мати .env.example, ніколи .env
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local

# Local Claude settings (per-user, не комітимо)
.claude/settings.local.json
**/.claude/settings.local.json

# Node
node_modules/
dist/
build/
*.log
npm-debug.log*

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Go
/bin/
/vendor/
*.test
*.out

# Rust
target/
Cargo.lock.bak

# Docker
.docker/

# Lecture 5.7 fixture — regenerated locally via setup_fixture.py
modules/5-claude-code-extended/5.7-sdk/fixture-repo/

# Lecture 5.7 sdk-python venv — bundled dev artifact, not source
modules/5-claude-code-extended/5.7-sdk/sdk-python/.venv/

# --- Module 9 collaboration demo sandboxes (rebuilt locally via make sandbox) ---
# Lecture 9.1 demo sandbox — rebuilt locally via setup.sh (make sandbox)
modules/9-collaboration/9.1-git-workflow/sandbox/

# Lecture 9.2 demo sandbox + w-helper worktree siblings + bare origin — rebuilt via make sandbox
modules/9-collaboration/9.2-git-worktrees/sandbox/
modules/9-collaboration/9.2-git-worktrees/sandbox-*/
modules/9-collaboration/9.2-git-worktrees/.sandbox-origin.git/

# Lecture 9.3 demo sandbox + worktree siblings + bare origin — rebuilt via make sandbox
modules/9-collaboration/9.3-worktree-merge-cleanup/sandbox/
modules/9-collaboration/9.3-worktree-merge-cleanup/sandbox-*/
modules/9-collaboration/9.3-worktree-merge-cleanup/.sandbox-origin.git/

# Lecture 9.4 demo sandbox — local seed (+ real GitHub origin), rebuilt via make sandbox/rules/perms
modules/9-collaboration/9.4-pull-requests/sandbox/

# Lecture 9.5 demo sandbox + bare origin — rebuilt via make sandbox (baseline main + feat/reminders PR)
modules/9-collaboration/9.5-code-review/sandbox/
modules/9-collaboration/9.5-code-review/.sandbox-origin.git/